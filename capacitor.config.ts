import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.robingenz.capacitor.onnxruntimedemo',
  appName: 'capacitor-onnx-runtime-demo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
